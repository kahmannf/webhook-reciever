//all hooks must be defined here. name the file 'hooks.json' and place it inside the root directory;
{
  "repohooks": [
    //example hook that will trigger on a push
    {
      "repository_id": "YOUR_RESPOSITORY_ID", //this command will only be executed for this repository
      "action": "command", //the action that will be executed
      "action_params": ["c:\\Utility\\RunBatInFolder.exe", "c:\\server\\online-recipe-manager","pull.bat"] // the parameters for the "command" action
    }
  ],
  "otherhooks": []
}
